<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:gravity="center_horizontal"
    android:textStyle="bold">

    <ImageView
        android:src="@drawable/about"
        android:id="@+id/tab_img"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:layout_width="20dp"
        android:layout_height="20dp"/>

    <TextView
        android:id="@+id/tab_txt"
        android:text="About"
        android:fontFamily="@font/happy_times_at_the_ikob_regular"
        app:layout_constraintTop_toTopOf="@id/tab_img"
        app:layout_constraintBottom_toBottomOf="@id/tab_img"
        app:layout_constraintStart_toEndOf="@id/tab_img"
        android:layout_marginStart="32dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</androidx.constraintlayout.widget.ConstraintLayout>
